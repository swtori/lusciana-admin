services:
  backend:
    build:
      context: ./backend
    container_name: lusciana-backend
    env_file:
      - ./backend/.env
    environment:
      APP_ENV: production
      APP_URL: https://api.ton-domaine.tld
      FRONTEND_URL: https://ton-domaine.tld
      MONGODB_URI: mongodb://luna:btKXJg5cSrdh6hWY04PslfyuXsAOhB@mongodb:27017/lusciana?authSource=admin
      MONGODB_DATABASE: lusciana
    ports:
      - "4000:4000"
    depends_on:
      - mongodb
    restart: always
